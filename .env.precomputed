# Pre-computed Embeddings (DEFAULT - No API Needed)
# Use this for Day 1 or offline workshops

EMBEDDING_PROVIDER=precomputed
EMBEDDING_FALLBACK=true

# No API keys required
# Uses pre-computed vectors from database seed data

# Run with:
# ./mvnw spring-boot:run
