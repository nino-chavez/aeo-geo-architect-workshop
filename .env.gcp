# Google Vertex AI (GCP)
# Use this if your organization has Google Cloud Platform

EMBEDDING_PROVIDER=vertex-ai
GCP_PROJECT_ID=your-project-id
GCP_LOCATION=us-central1

# Prerequisites:
# 1. Install gcloud CLI: https://cloud.google.com/sdk/docs/install
# 2. Authenticate: gcloud auth application-default login
# 3. Enable API: gcloud services enable aiplatform.googleapis.com

# Run with:
# cp .env.gcp .env
# ./mvnw spring-boot:run -Dspring-boot.run.profiles=vertex-ai
